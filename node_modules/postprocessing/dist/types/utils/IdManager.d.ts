/**
 * An ID manager.
 *
 * @category Utils
 */
export declare class IdManager {
    /**
     * The next ID.
     */
    private nextId;
    /**
     * Constructs a new ID manager.
     *
     * @param initialId - The first ID.
     */
    constructor(initialId?: number);
    /**
     * Returns the next unique ID.
     *
     * @return The ID.
     */
    getNextId(): number;
    /**
     * Resets the ID counter.
     *
     * @param initialId - The first ID.
     * @return This manager.
     */
    reset(initialId?: number): this;
}
