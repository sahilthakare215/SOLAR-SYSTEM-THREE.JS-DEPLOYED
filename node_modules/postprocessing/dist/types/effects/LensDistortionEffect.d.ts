import { Vector2 } from "three";
import { Effect } from "./Effect.js";
/**
 * LensDistortionEffect options.
 *
 * @category Effects
 */
export interface LensDistortionEffectOptions {
    /**
     * The radial distortion coefficients.
     *
     * @defaultValue (0, 0)
     */
    distortion?: Vector2;
    /**
     * The principal point.
     *
     * @defaultValue (0, 0)
     */
    principalPoint?: Vector2;
    /**
     * The focal length.
     *
     * @defaultValue (1, 1)
     */
    focalLength?: Vector2;
    /**
     * The skew factor in radians.
     *
     * @defaultValue 0
     */
    skew?: number;
}
/**
 * A lens distortion effect.
 *
 * Original shader ported from https://github.com/ycw/three-lens-distortion
 */
export declare class LensDistortionEffect extends Effect implements LensDistortionEffectOptions {
    /**
     * Constructs a new lens distortion effect.
     *
     * @param options - The options.
     */
    constructor({ distortion, principalPoint, focalLength, skew }?: LensDistortionEffectOptions);
    get distortion(): Vector2;
    set distortion(value: Vector2);
    get principalPoint(): Vector2;
    set principalPoint(value: Vector2);
    get focalLength(): Vector2;
    set focalLength(value: Vector2);
    get skew(): number;
    set skew(value: number);
}
