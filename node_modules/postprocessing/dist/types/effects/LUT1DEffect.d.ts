import { Texture } from "three";
import { Effect } from "./Effect.js";
/**
 * LUT1DEffect options.
 *
 * @category Effects
 */
export interface LUT1DEffectOptions {
    /**
     * The LUT.
     */
    lut?: Texture | null;
}
/**
 * A 1D LUT effect.
 *
 * @see https://affinityspotlight.com/article/1d-vs-3d-luts/
 * @category Effects
 */
export declare class LUT1DEffect extends Effect implements LUT1DEffectOptions {
    /**
     * Constructs a new LUT effect.
     *
     * @param options - The options.
     */
    constructor({ lut }?: LUT1DEffectOptions);
    /**
     * Indicates whether the LUT uses high precision.
     */
    private get lutPrecisionHigh();
    private set lutPrecisionHigh(value);
    get lut(): Texture | null;
    set lut(value: Texture | null);
}
