import { Effect } from "./Effect.js";
/**
 * ScanlineEffect options.
 *
 * @category Effects
 */
export interface ScanlineEffectOptions {
    /**
     * The scanline density.
     *
     * @defaultValue 1.0
     */
    density?: number;
    /**
     * A scanline offset in the range [0.0, 1.0].
     *
     * @defaultValue 0.0
     */
    offset?: number;
    /**
     * The scanline scroll speed.
     *
     * @defaultValue 0.0
     */
    scrollSpeed?: number;
}
/**
 * A scanline effect.
 *
 * Based on an implementation by Georg 'Leviathan' Steinrohder (CC BY 3.0):
 * http://www.truevision3d.com/forums/showcase/staticnoise_colorblackwhite_scanline_shaders-t18698.0.html
 *
 * @category Effects
 */
export declare class ScanlineEffect extends Effect implements ScanlineEffectOptions {
    /**
     * @see {@link density}
     */
    private _density;
    /**
     * Constructs a new scanline effect.
     *
     * @param options - The options.
     */
    constructor({ density, offset, scrollSpeed }?: ScanlineEffectOptions);
    get density(): number;
    set density(value: number);
    get offset(): number;
    set offset(value: number);
    get scrollSpeed(): number;
    set scrollSpeed(value: number);
    protected onResolutionChange(): void;
}
