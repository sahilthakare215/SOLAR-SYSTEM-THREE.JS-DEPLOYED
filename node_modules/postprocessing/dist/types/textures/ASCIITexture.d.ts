import { CanvasTexture } from "three";
/**
 * ASCIITexture options.
 *
 * @category Textures
 */
export interface ASCIITextureOptions {
    /**
     * A string of characters to use for the lookup texture.
     *
     * @defaultValue " .:,'-^=*+?!|0#X%WM@"
     */
    characters?: string;
    /**
     * The font to use.
     *
     * @defaultValue "Arial"
     */
    font?: string;
    /**
     * The font size in pixels.
     *
     * @defaultValue 54
     */
    fontSize?: number;
    /**
     * The texture size.
     *
     * @defaultValue 1024
     */
    size?: number;
    /**
     * The amount of cells along each side of the texture.
     *
     * @defaultValue 16
     */
    cellCount?: number;
}
/**
 * An ASCII character lookup texture.
 *
 * @category Textures
 */
export declare class ASCIITexture extends CanvasTexture {
    /**
     * The amount of characters in this texture.
     */
    readonly characterCount: number;
    /**
     * The amount of cells along each side of the texture.
     */
    readonly cellCount: number;
    /**
     * Constructs a new ASCIITexture.
     */
    constructor({ characters, font, fontSize, size, cellCount }?: ASCIITextureOptions);
}
