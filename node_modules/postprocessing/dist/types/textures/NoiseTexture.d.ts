import { DataTexture } from "three";
/**
 * A noise texture.
 *
 * @category Textures
 */
export declare class NoiseTexture extends DataTexture {
    /**
     * Constructs a new noise texture.
     *
     * Supported texture formats: `RGBAFormat`, `RedFormat`, `RGFormat`.
     *
     * @param width - The width.
     * @param height - The height.
     * @param format - The texture format. Default is `RGBAFormat`.
     * @param type - The texture type.
     */
    constructor(width: number, height: number, format?: 1023, type?: 1009);
}
